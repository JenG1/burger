<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if devoured}}YUM{{else}}DEVOUR!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
